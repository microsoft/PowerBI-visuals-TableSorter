{
  "name": "@essex/tablesorter-react",
  "version": "1.0.1",
  "description": "",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint 'src/**/*[!.d].{ts,tsx}'",
    "karma:tdd": "karma start --tdd",
    "karma:once": "karma start",
    "docs": "typedoc --out docs --ignoreCompilerErrors ./ --externalPattern **/node_modules/**/*.* --exclude **/src/**/*.spec.ts > /dev/null || exit 1",
    "test": "npm run clean && tsc && npm-run-all lint karma:once docs"
  },
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "@essex/tablesorter": "^1.0.1"
  }
}
