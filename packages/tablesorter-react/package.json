{
  "name": "@essex/tablesorter-react",
  "version": "0.10.9",
  "description": "",
  "private": true,
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "node ./scripts/compile",
    "lint": "tslint \"src/**/*[!.d].{ts,tsx}\"",
    "karma:tdd": "karma start --tdd",
    "karma:once": "karma start",
    "docs": "typedoc --out docs --ignoreCompilerErrors ./ --externalPattern \"**/node_modules/**/*.*\" --exclude \"**/src/**/*.spec.ts\"",
    "test": "npm run clean && npm run compile && npm-run-all lint karma:once docs"
  },
  "author": "Microsoft",
  "license": "MIT",
  "dependencies": {
    "@essex/tablesorter": "^0.10.9"
  },
  "devDependencies": {
    "phantomjs-prebuilt": "^2.1.7"
  }
}
